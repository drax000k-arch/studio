/**
 * @fileoverview Firestore Security Rules for Advisify AI.
 *
 * Core Philosophy:
 * This ruleset enforces a strict user-ownership model for user data, decision scenarios, and decision options.
 * Comments are stored in a top-level collection with the scenario ID and author ID denormalized, enabling more flexible queries while maintaining ownership control.
 *
 * Data Structure:
 * - /users/{userId}: Stores user profiles.
 * - /users/{userId}/decisionScenarios/{decisionScenarioId}: Stores decision scenarios created by users.
 * - /users/{userId}/decisionScenarios/{decisionScenarioId}/options/{decisionOptionId}: Stores options for each scenario.
 * - /comments/{commentId}: Stores comments on decision scenarios.
 *
 * Key Security Decisions:
 * - Users can only access their own user documents and the decision scenarios and options nested within their user document.
 * - Comments are stored in a top-level collection but are secured to ensure that the `userId` and `scenarioId` fields are valid and that the comment author can delete their own comments.
 * - List operations are generally restricted to the owner of the data, except for the comments collection, where listing is allowed (but creating is not) to support displaying comments on a scenario.
 *
 * Denormalization for Authorization:
 * - The `Comment` entity includes both `userId` and `scenarioId` to allow for simpler authorization rules without needing to perform additional `get()` operations to retrieve the user or scenario.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Defines a helper function to check if the current user is signed in.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * @description Defines a helper function to check if the authenticated user ID matches the provided user ID.
     */
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * @description Defines a helper function to check if the authenticated user is the existing owner of the resource.
     */
    function isExistingOwner(userId) {
      return isSignedIn() && request.auth.uid == resource.data.userId;
    }

    /**
     * @description Security rules for the /users/{userId} collection.
     * @path /users/{userId}
     * @allow (create) User with ID 'user123' creates their profile: auth.uid == 'user123'
     * @deny (create) User with ID 'user456' attempts to create a profile for 'user123': auth.uid != 'user123'
     * @allow (get, update, delete) User with ID 'user123' reads/updates/deletes their own profile: auth.uid == 'user123'
     * @deny (get, update, delete) User with ID 'user456' attempts to read/update/delete profile for 'user123': auth.uid != 'user123'
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId} {
      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if isOwner(userId);
      allow delete: if isOwner(userId);

            match /decisionScenarios/{decisionScenarioId} {
                allow get: if isOwner(userId);
                allow list: if isOwner(userId);
                allow create: if isOwner(userId);
                allow update: if isOwner(userId);
                allow delete: if isOwner(userId);

                match /options/{decisionOptionId} {
                    allow get: if isOwner(userId);
                    allow list: if isOwner(userId);
                    allow create: if isOwner(userId);
                    allow update: if isOwner(userId);
                    allow delete: if isOwner(userId);
                }
            }
    }

    /**
     * @description Security rules for the /comments/{commentId} collection.
     * @path /comments/{commentId}
     * @allow (get, list) Any user can get and list comments.
     * @allow (create) Not allowed directly, must go through a backend function to validate scenarioId.
     * @allow (update) Not allowed.
     * @allow (delete) Only the owner can delete their own comment.
     * @principle Allows public read access, but enforces ownership for deletes.
     */
    match /comments/{commentId} {
      allow get: if true;
      allow list: if true;
      allow create: if false;
      allow update: if false;
      allow delete: if isSignedIn() && resource.data.userId == request.auth.uid;
    }
  }
}